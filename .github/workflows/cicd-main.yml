name : CICD main
on: 
  # push:
  #   branches:
  #     - main
  workflow_dispatch:
jobs:
  reusable_workflow:
    uses: DevOpsProjectsOrganization/roboshop-github-actions/.github/workflows/ci-main.yml@main
    with :
      app_type : python
      app_name: payment
      app_env : dev
      enabled : false
      ingress : false